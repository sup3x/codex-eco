<!-- GENERATED by scripts/build-skills.mjs from plugins/eco/skills/eco/SKILL.md.
     Do not edit here; edit the rules block in that file and re-run
     `npm run build:skills`. CI fails if the two drift.

     Append this block to your project's AGENTS.md.

     Why this file is the primary way to run eco on Codex, and the skill is the
     secondary one: Codex puts only a skill's NAME and DESCRIPTION in the prompt.
     The body lives on disk and the agent has to read it with a shell command
     before the rules can apply. That read is a full extra round trip, and a round
     trip re-sends the entire prefix - measured at ~16k input tokens on a fresh
     thread, more than a short answer costs in output. AGENTS.md has no such cost:
     Codex loads it into the prompt itself, on every turn, at every reasoning
     effort, on every model, in the CLI and in the ChatGPT desktop app alike.

     Keep it short anyway. AGENTS.md is re-sent every turn (cached, but not free)
     and `project_doc_max_bytes` silently truncates whatever exceeds its cap. -->

<!-- codex-eco:start -->
## Token discipline

Same outcomes, minimum tokens. Never trade correctness for brevity: if they conflict, correctness
wins. Always reply in the user's language.

{{RULES}}
<!-- codex-eco:end -->
